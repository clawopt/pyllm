# Pandas 教程

Pandas 是 Python 数据分析的核心库，提供了高效的数据结构和数据分析工具。

## 安装

```bash
pip install pandas
```

## 基本使用

```python
import pandas as pd

s = pd.Series([1, 2, 3, 4, 5])
df = pd.DataFrame({'name': ['Alice', 'Bob'], 'age': [25, 30]})
print(df)
```
